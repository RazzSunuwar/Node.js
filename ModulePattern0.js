let counter = function(arr){
    return "There are "+ arr.length + "numbers of elements in the array"
};

let sum = function(a, b){
    return `The total of elements is ${a+b}`
};

let PI = 3.14;

module.exports = { counter, sum, PI };

